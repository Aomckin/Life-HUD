async function request(url, options) { const response = await fetch(url, options); const text = await response.text(); let body = {}; try { body = text ? JSON.parse(text) : {}; } catch { throw new Error("??????????"); } if (!response.ok) throw new Error(body.message || `?????${response.status}?`); return body; }
export const api = { state: () => request("/state"), events: (limit = 8) => request(`/api/life-events?limit=${limit}`), command: (type, payload = {}) => request("/command", {method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({type,payload})}) };
